name: tick-version
version: 0.0.5
summary: Increment your project's patch version for NPM based projects
keywords:
  - git
  - version
  - npm
  - patch
properties:
  - name: token
    type: string
    required: true
